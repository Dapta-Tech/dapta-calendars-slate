{
  "name": "@slate/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared domain types + zod contracts (the web↔api boundary).",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "echo \"(types) no tests\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@slate/engine": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@slate/config": "workspace:*",
    "eslint": "^10.8.0",
    "typescript": "^7.0.2"
  }
}
